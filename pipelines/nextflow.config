docker {
   enabled = true
   runOptions = '--gpus=all'
}
plugins = ['nf-prov']
prov {
   enabled = true
   formats {
      wrroc {
         file = 'ro-crate-metadata.json'
         overwrite = true
      }
   }
}
workDir = '../.work'
